

export const ENDPOINT  = "http://localhost:3000/"

export const categories = () => (`${ENDPOINT}/categories`)
export const products = () => (`${ENDPOINT}/products`)
export const banners = () => (`${ENDPOINT}/banners`)
export const addToCart = ()=>(`${ENDPOINT}/addToCart`)

